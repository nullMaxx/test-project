# Web Framework & Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0

# Database / ORM (optional / if migrating from in-memory dictionary)
# sqlalchemy>=2.0.0

# Testing & API Client (recommended for testing FastAPI endpoints)
# pytest>=8.0.0
# httpx>=0.27.0
